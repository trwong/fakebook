import { React } from 'react';
import { connect } from 'react-redux';
import FeedLikeDisplay from './feed_like_display';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(FeedLikeDisplay);