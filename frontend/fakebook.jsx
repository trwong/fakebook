import React from 'react';
import ReactDOM from 'react-dom';

// imports for window testing

document.addEventListener("DOMContentLoaded", () => {
    const root = document.getElementById('root');
    ReactDOM.render(<div>fakebook</div>, root);


    // window testing
   
});